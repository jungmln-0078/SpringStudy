<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.MemberMapper">
    <select id="getMembers" resultType="example.mybatis.blog.model.Member">
        SELECT mid, email, nickname
        FROM member
    </select>
    <select id="getMemberByPk" parameterType="hashMap" resultType="example.mybatis.blog.model.Member">
        SELECT mid, email, nickname
        FROM member
        WHERE email = #{email} AND nickname = #{nickname}
    </select>
    <select id="getMemberByPw" parameterType="hashMap" resultType="example.mybatis.blog.model.Member">
        SELECT mid, email, nickname
        FROM member
        WHERE email = #{email} AND password = #{password}
    </select>
    <insert id="joinMember" parameterType="hashMap" useGeneratedKeys="true" keyProperty="mid">
        INSERT INTO member (email, nickname, password)
        VALUES (#{email}, #{nickname}, #{password})
    </insert>
    <update id="updateMember" parameterType="hashMap">
        UPDATE member
        SET email = #{email}, nickname = #{nickname}, password = #{password}
        WHERE mid = #{mid}
    </update>
    <delete id="deleteMember" parameterType="hashMap">
        DELETE FROM member
        WHERE mid = #{mid}
    </delete>
</mapper>
